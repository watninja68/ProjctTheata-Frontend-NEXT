"use client"; // LandingPage uses hooks (useState, useEffect) and react-router's Link

import LandingPage from '@/components/LandingPage';

export default function Home() {
  return <LandingPage />;
}
